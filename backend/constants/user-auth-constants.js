const TOKEN_EXPIRE_TIME = 3600;
const SALT_LENGTH = 10;

const MIN_PASSWORD_LENGTH = 6;
const MAX_PASSWORD_LENGTH = 30;
const SERVICE_PROVIDER = "SERVICE_PROVIDER";
const CLIENT = "CLIENT";
const USER_TYPES = { SERVICE_PROVIDER, CLIENT };

module.exports = {
  TOKEN_EXPIRE_TIME,
  SALT_LENGTH,
  MIN_PASSWORD_LENGTH,
  MAX_PASSWORD_LENGTH,
  USER_TYPES,
  CLIENT,
  SERVICE_PROVIDER
};